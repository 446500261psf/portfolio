# 部署到 GitHub Pages（Settings → Pages → Source: GitHub Actions）
name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  # 避免连续 push 时正在跑的部署被 cancel，导致“永远红”
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install & build
        run: |
          REPO_NAME="${{ github.event.repository.name }}"
          if [[ "$REPO_NAME" == *.github.io ]]; then
            export BASE_PATH=/
          else
            export BASE_PATH="/$REPO_NAME/"
          fi
          npm ci
          npm run build

      # Cove Watch 模拟器是独立 Vite 应用，发布到 <site>/simulator/。
      # 它的 vite base 是 './'，相对路径在任意子目录都成立，无需注入 BASE_PATH。
      - name: Build Cove Watch simulator
        run: |
          cd visual-watch/apps/simulator
          npm ci
          npm run build
          mkdir -p "$GITHUB_WORKSPACE/dist/simulator"
          cp -r dist/. "$GITHUB_WORKSPACE/dist/simulator/"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      # 不在此处引用 steps.*，避免部分运行器在 job 级解析 environment 时失败
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
