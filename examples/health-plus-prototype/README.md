# Explore Health+ 原型

独立静态预览（不覆盖作品集主站）。

## 本机打开

在仓库根目录：

```bash
npm install
npm run dev
```

浏览器打开：http://localhost:5173/health-plus.html

或只预览本目录：

```bash
npx serve examples/health-plus-prototype
```

## 交互

滚轮 / 滑动 / 方向键切换卡片。
